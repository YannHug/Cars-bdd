{% extends "base.html.twig" %}

{% block body %}

<h1>Ajouter une marque</h1>

{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.name, {label: 'Marque :'}) }}
    <button class="btn btn-primary">Ajouter</button>
{{ form_end(form) }}
    <a href="{{ path('car_browse') }}">Retour à la liste</a>
{% endblock %}