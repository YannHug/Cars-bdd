{% extends 'base.html.twig' %}

{% block title %}Les voitures
{% endblock %}
{% block body %}
	<div class="container">
		<h1 class="mt-3">{{car.brand.name}}
			{{car.model}}</h1>
		<div class="row col-12 px-2 d-flex">
			<div class="card-body">
				<p class="card-text">Date de fabrication :
					{{car.createdAt|date('d-m-Y')}}</p>
			</div>
		</div>
	</div>
    <a href="{{ path('car_browse') }}">Retour à la liste</a>
{% endblock %}
